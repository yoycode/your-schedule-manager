import { createStore } from 'vuex'
import { Schedule } from '@/store/modules/Schedule'

export default createStore({
  state: {
    test: "hihitest"
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    Schedule
  }
})
